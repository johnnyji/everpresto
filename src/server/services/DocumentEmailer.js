import CollectionsSocket from '../sockets/collections';
import NodeMailer from 'node-mailer';

/*
  The Emailer that is in charge of all Document related emails
 */
export default {

  /**
   * Sends the document signing links to signers initially
   * @param  {Array} docs [description]
   */
  sendInitialEmails(docs) { 
        
  } 


};